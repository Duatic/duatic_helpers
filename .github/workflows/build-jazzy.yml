name: Jazzy Build Main
on:
  workflow_dispatch:
  workflow_call:

jobs:
  jazzy_binary_main:
    uses: ./.github/workflows/reusable_ici.yml
    with:
      ros_distro: jazzy
      ros_repo: main
      upstream_workspace: ''
      ref_for_scheduled_build: main
